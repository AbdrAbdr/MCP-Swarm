> <img src="https://flagcdn.com/20x15/gb.png" alt="EN" /> [Read in English](./CHANGELOG.md)

# Changelog

Все значимые изменения в проекте MCP Swarm документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
версионирование следует [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [1.1.3] - 2026-02-09

### Что нового

#### 📱 Интеграция Telegram-бота
- **Полные Telegram-уведомления** — События задач, статусы агентов, ошибки CI, код-ревью — всё приходит в Telegram.
- **Двуязычная инструкция по настройке** — Полный `TELEGRAM.md` с пошаговыми инструкциями на английском и русском.
- **Поддержка @userinfobot** — Простой способ узнать свой Telegram User ID.
- **Команды бота** — `/start`, `/projects`, `/status`, `/agents`, `/tasks`, `/myid`, `/reviews`, `/approve`, `/reject`.
- **Переменные окружения** — `TELEGRAM_USER_ID` и `TELEGRAM_BOT_URL` для всех конфигураций MCP.

#### 🏗️ Качество кода и безопасность
- **ESLint + Prettier** — Полная настройка линтинга и форматирования с `typescript-eslint`. Скрипты: `lint`, `lint:fix`, `format`, `format:check`.
- **fs-sandbox** — Песочница файловой системы (`src/fsSandbox.ts`) предотвращает path-traversal атаки, ограничивая операции агентов границами проекта.
- **Рефакторинг Dashboard** — 133-строчный inline HTML извлечён из `companion.ts` в модуль `dashboard.ts`.

#### 🔭 Наблюдаемость и управление
- **Файловое логирование** — Логи companion в `~/.mcp-swarm/logs/companion-YYYY-MM-DD.log` с ротацией 7 дней.
- **`mcp-swarm-doctor`** — CLI-диагностика: Node.js, Git, статус companion, порты, логи, Hub URL, конфиги IDE.
- **Интерактивный Dashboard** — Кнопки Pause/Resume/Shutdown + Toast-уведомления на `http://localhost:37373`.
- **Auto-Update Notifier** — Предупреждает при запуске, если доступна новая версия в npm.

#### 🐝 Web Dashboard
- **Dark-theme дашборд** на `http://localhost:37373` с автообновлением каждые 5с.
- **PID-файл** + **Graceful shutdown** — `~/.mcp-swarm/companion.pid` с обработчиками SIGTERM/SIGINT.
- **`/health` endpoint** — `{ ok, pid, uptime }` для мониторинга.
- **Unit-тесты** — Тесты `normalizeGitRemote` и PID-файл.

---

### Конфигурация

**Вариант A: Remote (Рекомендуемый)**

```json
{
  "mcpServers": {
    "mcp-swarm": {
      "command": "npx",
      "args": [
        "-y", "-p", "mcp-swarm",
        "mcp-swarm-remote",
        "--url", "https://mcp-swarm-server.YOUR-SUBDOMAIN.workers.dev/mcp"
      ],
      "env": {
        "SWARM_HUB_URL": "wss://mcp-swarm-hub.YOUR-SUBDOMAIN.workers.dev/ws",
        "TELEGRAM_USER_ID": "ВАШ_TELEGRAM_USER_ID",
        "TELEGRAM_BOT_URL": "https://YOUR-TELEGRAM-BOT.workers.dev"
      }
    }
  }
}
```

**Вариант B: Локальный с Hub**

```json
{
  "mcpServers": {
    "mcp-swarm": {
      "command": "node",
      "args": ["C:/path/to/Swarm_MCP/dist/serverSmart.js"],
      "env": {
        "SWARM_HUB_URL": "wss://mcp-swarm-hub.YOUR-SUBDOMAIN.workers.dev/ws",
        "TELEGRAM_USER_ID": "ВАШ_TELEGRAM_USER_ID",
        "TELEGRAM_BOT_URL": "https://YOUR-TELEGRAM-BOT.workers.dev"
      }
    }
  }
}
```

| Переменная | Обязательна | Описание |
|------------|-------------|----------|
| `SWARM_HUB_URL` | ✅ | WebSocket URL вашего Hub воркера |
| `TELEGRAM_USER_ID` | Опционально | Ваш Telegram User ID (получить через [@userinfobot](https://t.me/userinfobot)) |
| `TELEGRAM_BOT_URL` | Опционально | URL вашего Telegram-бот воркера |

> 📱 См. [TELEGRAM.md](./TELEGRAM.md) для полной инструкции по настройке Telegram.

---

### Ключевые возможности платформы

Лучшие возможности MCP Swarm, реализованные за всё время разработки:

#### 🛠 26 Smart Tools
Консолидированы из 54 инструментов — без потери функций, 2× меньше IDE-слотов. Каждый инструмент использует параметр `action` для множества операций.

#### 🧠 MoE Router — 19 AI-моделей
Интеллектуальная маршрутизация моделей с оптимизацией стоимости. Поддержка Anthropic (Claude Opus 4.6), OpenAI (GPT-5.3 Codex), Google (Gemini 3), Moonshot (Kimi K2.5).

#### 🛡️ AIDefence
Обнаружение угроз за <10мс: prompt injection, jailbreak, code injection, data exfiltration, social engineering. Настраиваемые уровни чувствительности.

#### 🤝 Распределённый консенсус
Raft-подобное лидер-избрание, BFT-режим, система предложений с настраиваемыми порогами голосования.

#### 🔍 HNSW Векторный поиск
В 150×–12 500× быстрее перебора. Чистый TypeScript, cosine/euclidean/dot product.

#### 🌐 Cloudflare Workers
Self-hosted инфраструктура: Hub, MCP Server, Telegram Bot — всё на Cloudflare Free Tier.

#### 🔄 Полное покрытие Bridge
Все 26 Smart Tools работают через Remote Bridge. Универсальная делегация через `toolName.startsWith("swarm_")`.

#### 📦 Установка в один клик
`npx mcp-swarm-install` — авто-обнаружение IDE, мерж конфигов, поддержка `--telegram-user-id`.

#### 🚀 Smart Router и память
Оптимизация стоимости (Opus → Sonnet даунгрейд), семантический кэш, 3-уровневая гибридная система памяти.

#### 👥 Agent Teams и Skills
Мульти-агентная координация с ролями. Кросс-IDE обнаружение навыков (Gemini, Claude, Cursor, Windsurf, Codex).

---

### Полный changelog

Полный changelog по всем версиям доступен в [GitHub Releases](https://github.com/AbdrAbdr/MCP-Swarm/releases).
