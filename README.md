# 🐝 MCP Swarm v0.9.0

**Multi-Agent Coordination Platform** — Глобальный MCP-сервер для координации AI-агентов (Claude, Cursor, Windsurf, OpenCode) в любом вашем проекте.

---

## 🚀 Как это работает?

1.  **Устанавливаете один раз** в систему (через конфиг IDE).
2.  **Открываете любой проект** и говорите агенту: *"Используй MCP Swarm"*.
3.  **Сервер сам инициализируется:** создаст правила (`CLAUDE.md`, `GEMINI.md` и др.) и папки для координации.
4.  **Агенты работают сообща:** блокируют файлы, передают задачи и общаются через общий Cloudflare Hub.

---

## 🛠 1. Глобальная установка

Сначала склонируйте и соберите сервер на вашей машине:

```bash
git clone https://github.com/AbdrAbdr/Swarm_MCP.git
cd Swarm_MCP
npm install
npm run build
```

---

## ⚙️ 2. Добавление в IDE (Конфигурация)

Добавьте следующий JSON в файл настроек вашей IDE. 

> **Важно:** Замените `/ПУТЬ/К/Swarm_MCP` на реальный абсолютный путь к папке, куда вы склонировали репозиторий.

### 🪟 Windows
**Пути конфигов:**
- **Claude Desktop:** `%APPDATA%\Claude\claude_desktop_config.json`
- **Windsurf:** `~/.codeium/windsurf/mcp_config.json`
- **Antigravity:** `%APPDATA%\antigravity\mcp_config.json`

```json
{
  "mcpServers": {
    "mcp-swarm": {
      "command": "node",
      "args": ["C:/ПУТЬ/К/Swarm_MCP/dist/serverSmart.js"],
      "env": {
        "SWARM_HUB_URL": "wss://mcp-swarm-hub.unilife-ch.workers.dev/ws",
        "SWARM_PROJECT": "default"
      }
    }
  }
}
```

### 🍎 macOS
**Пути конфигов:**
- **Claude Desktop:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Cursor:** `~/.cursor/mcp.json`
- **Windsurf:** `~/.windsurf/mcp_config.json`

```json
{
  "mcpServers": {
    "mcp-swarm": {
      "command": "node",
      "args": ["/Users/ИМЯ/path/to/Swarm_MCP/dist/serverSmart.js"],
      "env": {
        "SWARM_HUB_URL": "wss://mcp-swarm-hub.unilife-ch.workers.dev/ws",
        "SWARM_PROJECT": "default"
      }
    }
  }
}
```

### 🐧 Linux
**Пути конфигов:**
- **OpenCode:** `~/.config/opencode/opencode.json`

```json
{
  "mcp": {
    "mcp-swarm": {
      "type": "local",
      "command": ["node", "/home/USER/path/to/Swarm_MCP/dist/serverSmart.js"],
      "enabled": true,
      "environment": {
        "SWARM_HUB_URL": "wss://mcp-swarm-hub.unilife-ch.workers.dev/ws",
        "SWARM_PROJECT": "default"
      }
    }
  }
}
```

---

## 📂 3. Использование в проекте

Вам больше не нужно ничего создавать вручную!

1. Откройте любой проект в вашей IDE.
2. Напишите агенту: **"Используй MCP Swarm для этого проекта"**.
3. Агент вызовет `swarm_agent({ action: "register", repoPath: "..." })`.
4. **Сервер автоматически:**
   - Увидит, что проект новый.
   - Создаст файлы правил: `CLAUDE.md`, `GEMINI.md`, `AGENT.md`, `AGENTS.md`.
   - Создаст скрытые папки `swarm/` и `orchestrator/`.
   - Если файлы уже были — просто добавит инструкции в конец.

---

## 🧠 Почему это круто?

- **Один сервер для всех проектов:** Не нужно копировать код сервера в каждую папку.
- **Авто-обучение агентов:** Как только сервер создает `.md` файлы, агент сразу понимает, как пользоваться всеми 41 инструментами.
- **Кросс-платформенность:** Команда на Windows и команда на Mac работают в одном пространстве через Cloudflare.

---

## 🛡️ Безопасность

- **repoPath:** Обязательный параметр. Агент сам определяет путь к текущей папке.
- **File Locks:** Предотвращают одновременное редактирование файлов.
- **Voting:** Для опасных операций требуется согласие других агентов.

---
© 2026 MCP Swarm Team | MIT License
